WIDTH, HEIGHT = 800, 800               -- dimensiones de pantalla
W, H = 0, 0                            -- dimensiones de cada casilla (WIDTH / Board.x, HEIGHT / Board.y)
COLUMNS, ROWS = nil, nil               -- cuantas columnas y filas hay
DEFAULT_COLUMNS, DEFAULT_ROWS = 30, 30 -- dimensiones por defecto
FRAMES = nil                           -- numero de frames que guardar si se especifica (si es nil no se para)
VEL = 1                                -- steps de simulacion por segundo	
